global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'hls-streaming-service'
    metrics_path: '/management/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8888']

    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/token
